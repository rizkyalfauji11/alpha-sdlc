# Authentication & Token Handling — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> How the app **authenticates and handles tokens at runtime** — obtain, store, attach, **refresh**,
> expire, and revoke — the mechanics that cause 401 loops, expired-token bugs, and refresh races
> when they drift. This owns the *handling*; it cross-links rather than duplicates:
> **policy/compliance → `12-security-compliance.md`**, **per-service auth *method* → `15-api-reference.md`**,
> **token *names* / where config lives → `09-environment.md`**. **No secret values** — names/locations only.
>
> Tiered: only for apps that authenticate. Skip (and say so) for a public/no-auth app.

## Scheme

<Auth model — session cookie · JWT bearer · OAuth2 / OIDC (which grant) · API key · mTLS. Identity provider if any (Keycloak, Auth0, Cognito, in-house). Link the login/authorize flow.>

## Tokens

| Token | Type | Lifetime | Where obtained | Storage (per platform) | Attached to requests via |
|-------|------|----------|----------------|------------------------|--------------------------|
| <access> | JWT | <15 min> | <`POST /auth/login`> | <web: memory / httpOnly cookie · Android: EncryptedSharedPreferences · iOS: Keychain> | <`Authorization: Bearer` interceptor> |
| <refresh> | opaque | <30 days> | <login / `POST /auth/refresh`> | <httpOnly cookie / secure storage> | <refresh call only, never on normal requests> |

## Refresh flow

<When and how the access token is refreshed: proactively (before expiry, allowing clock skew) or reactively (on 401). Refresh-token **rotation** (is a new refresh token issued each time? old one invalidated?). Where the refresh logic lives (interceptor/middleware file).>

- **Concurrent refresh (single-flight):** <how simultaneous 401s are handled — one refresh in flight, other requests queue and retry with the new token; never N parallel refreshes. Name the mechanism/file, or flag if unhandled — this is a common race bug.>
- **Clock skew / early refresh:** <refresh N seconds before `exp`, if any.>

## 401 / expiry handling

<What happens on a 401: refresh-and-retry once, with a **retry cap** (never an infinite 401→refresh→401 loop); on refresh failure → logout + redirect to login. Where this is enforced (interceptor). Distinguish 401 (re-auth) from 403 (authorized-but-forbidden — no refresh).>

## Test / dev authentication

<How an automated test session (Boot & Smoke, integration/E2E, data-flow tests) authenticates **the real way**: the test account/user approach (dedicated seeded test user? per-run user?), where its credentials live (**env-var names / secret-manager location — never values**), and how a token is obtained in dev (the login call or helper). Without this, every test run improvises auth — or hardcodes a credential.>

## Logout, revocation & session invalidation

<How logout clears tokens (client + server revoke endpoint if any), what invalidates a session server-side, and how the client reacts to a revoked/blacklisted token.>

## Multi-tab / multi-device

<Web: how tabs share/sync auth state (storage events, broadcast channel) and avoid duplicate refreshes. Mobile: multiple devices, forced logout.>

## Gotchas

<The non-obvious ones: token in logs risk, CORS + credentialed requests, cookie SameSite/Secure flags, refresh on app resume, deep-link/SSO edge cases, biometric re-auth.>
