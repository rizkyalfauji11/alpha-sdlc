# Code Inventory — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> The searchable inventory of **every reusable code unit in the whole project — wherever it lives,
> not just the common/core module**: helpers, extensions, base classes, wrappers, formatters,
> validators, guards, custom hooks. This is what gives ladder **rung 2 ("already in this codebase?")
> teeth** — reuse becomes a lookup, not a grep-and-hope — and it's where duplication that already
> crept in becomes visible debt instead of hidden drift. **Search here before writing any helper or
> shared logic** (same discipline as assets/components/scaffolds/tokens); new reusable code is
> **registered on create** (`do-development`). Wiring *patterns* (how layers talk) live in
> [02-architecture](./02-architecture.md) → *Layer interaction & wiring* — this doc is the unit catalog.

## Reusable units

> One row per unit, **from a full-project scan — every module, not just `core/`**. *What it does*
> carries a plain phrase and the engineer signature. *Used by* is what makes safe change possible.

| Name | What it does (plain · engineer) | Kind | Location | Used by | Notes |
|------|--------------------------------|------|----------|---------|-------|
| <`formatMoney`> | <shows an amount as currency text · `(Long, Currency) -> String`> | helper | <`core/format/Money.kt`> | <features a, c> | |
| <`BaseListState`> | <standard loading/empty/error handling for any list screen · abstract state holder> | base class | <`feature-a/…` — **not in core**> | <feature-a only> | promotion candidate |
| <`useDebouncedQuery`> | <waits for typing to stop before searching · hook wrapping the query client> | hook/extension | <`web/src/lib/…`> | <search, list screens> | |

## Duplicates & contradictions — found, never smoothed over

> The same job implemented more than once (two date formatters, three validators) is a **bug source**
> (they drift apart) and a tech-debt candidate — record it, don't pick one silently.

| Job | Implementations (locations) | Differences | Resolution |
|-----|------------------------------|-------------|------------|
| <format date> | <`feature-a/DateUtil.kt` · `feature-b/date.ts`> | <locale handling differs> | <Open Decision → canonicalize; migration = `do-tech-debt-grooming`> |

## Common-code promotion rule

<When a unit moves into `core/common`: **only at the second real consumer** (rung 1 — no speculative
promotion), moved not copied, its inventory row updated (location + used-by), and consumers repointed
in the same change. What lives in `core/*` vs stays feature-local, and how modules consume core
(e.g. `api` vs `implementation` exposure) — per [02-architecture](./02-architecture.md).>

## Naming & discovery conventions

<How reusable units are named/placed so the next search finds them (suffixes, package/folder
conventions, one-job-per-unit). A unit the inventory can't describe in one plain phrase is probably
doing two jobs.>
