# UX Conventions — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> The app-wide **interaction behavior** standard — how common UX states/interactions are handled
> *consistently* across every screen (form validation, mandatory marking, empty/loading/error states,
> snackbars, confirmations). This owns interaction *behavior*; the **design system / components /
> theming live in `03-ui-architecture.md`** — cross-link, don't duplicate. Descriptive **and**
> prescriptive: capture the app's real patterns; where none exist, establish them with the user
> (or mark `UNKNOWN — needs human input`). Grooming references it (deviations → Open Decision),
> `do-development` builds to it, `do-testing` asserts it. Tiered: client apps only.

## Forms & validation

| Concern | Convention |
|---------|-----------|
| **Submit / CTA enable-disable** | <e.g. disabled until all mandatory fields valid; re-enables live as the user fixes> |
| **When validation runs** | <on blur · on change · on submit — and which> |
| **Inline error display** | <where the message shows (below field), style, when it clears> |
| **Mandatory indicator** | <e.g. red `*` after the label; and/or "Required" text — pick one and use it everywhere> |
| **Optional marking** | <e.g. "(optional)" suffix, or nothing> |
| **Double-submit prevention** | <button disables + shows spinner during the async call> |

## States

| State | Convention |
|-------|-----------|
| **Empty (no data yet)** | <illustration + explanatory text + primary CTA> |
| **No results (filter/search)** | <distinct from empty — "no matches", clear-filter affordance> |
| **Loading** | <skeleton vs spinner; where; initial vs refresh> |
| **Error** | <inline vs full-screen; retry affordance; message tone> |
| **Offline** | <banner / cached data / disabled actions — link `08-data-cache.md` if relevant> |

## Feedback

| Concern | Convention |
|---------|-----------|
| **Snackbar / toast** | <success vs error styling · duration · position · optional action (Undo) · one-at-a-time vs queue> |
| **Which events snackbar** | <e.g. save success, delete, network error — the standard set> |
| **Banner (persistent)** | <when a banner is used over a snackbar> |
| **Dialog vs bottom sheet** | <when each is used> |
| **Destructive-action confirmation** | <confirm dialog required for delete/irreversible; wording; primary/danger button placement> |
| **Optimistic vs pending** | <does the UI update optimistically or wait for the server?> |

## Interaction & affordance

| Concern | Convention |
|---------|-----------|
| **Disabled control** | <appearance + how the user learns *why* it's disabled (tooltip/helper text)> |
| **Async button** | <in-place spinner + disabled; label change?> |
| **Focus & keyboard** | <initial focus, enter-to-submit, tab order, focus after dialog close> |
| **Success feedback** | <snackbar vs inline vs navigation> |

## Lists, search & filters

| Concern | Convention |
|---------|-----------|
| **Search** | <debounce interval · min chars · server vs client search> |
| **Pagination** | <pagination vs infinite scroll vs load-more; page size> |
| **Refresh** | <pull-to-refresh / refresh affordance; auto-refetch on focus (→ [data-cache](./08-data-cache.md))> |
| **Sort & filter persistence** | <do selections survive navigation / app restart? where stored?> |
| **Live updates in a visible list** | <how a change arriving while the list is on screen appears — silent re-render, "new items" affordance, or on-next-visit (→ data-cache real-time map)> |
| **Formatting & localization (display)** | <dates, numbers, currency — locale, timezone, separators. Localized *data shape* → [conventions](./10-conventions.md) → data contract & localized rendering.> |

## Multi-step / wizard flows

> The standard for **stepped flows** (e.g. a create/configure flow: pick a parent item → configure it →
> pick related items → confirm). These break in predictable ways when there's no convention — capture it once.

| Concern | Convention |
|---------|-----------|
| **Where step state lives** | <one flow-level store holding all steps' data — not per-step local state that's lost on back> |
| **Per-step validation** | <each step validates before advancing; Next disabled until the step is valid> |
| **Navigation** | <Back preserves entered data · Cancel confirms discard · jump-to-step rules> |
| **Partial save / resume** | <is progress persisted (draft) so the user can leave and return? where?> |
| **Final commit** | <the flow commits **atomically** at the end — all-or-nothing — not per-step writes that leave half-created data> |
| **Cross-step dependencies** | <a later step that depends on an earlier choice (step 3's options depend on step 1's selection) refetches when that choice changes — see `08-data-cache.md` sync> |
| **Progress & errors** | <progress indicator (step N of M); how a mid-flow server error is recovered without losing entered data> |

## Accessibility

<Focus management on state change; snackbar/toast announced to screen readers (aria-live); validation errors associated with their field (aria-describedby); minimum tap target. Link `03-ui-architecture.md` theming/a11y if it holds more.>

## Gotchas

<The non-obvious app-specific ones: a screen that intentionally deviates (and why), platform differences (Android snackbar vs iOS toast vs web), any legacy pattern being migrated away from.>
