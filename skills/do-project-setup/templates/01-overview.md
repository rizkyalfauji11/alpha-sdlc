# Project Profile — <project name>

_Generated by `do-project-setup` · commit `<hash>` · approved <YYYY-MM-DD>_

> The baseline reference every skill grounds in. Volatile detail points to the
> authoritative file; each doc is stamped with the commit it was generated at.
> Refresh a doc when the repo drifts from its stamp.
> **Greenfield:** a doc written before the code exists is stamped `prescriptive (pre-code)` instead of a
> commit — it records *intended design, not observation*. Refresh re-stamps it against the real commit
> once the base is built, flagging anywhere the code diverged from the decision.

## Summary

<One paragraph: what this project/repo is, its platform(s), and its role in the wider system.>

## Org settings

> Decided once at setup (each its own gate), honored by every skill. Machine mirror written to
> `docs/basics/.alpha-sdlc.json` for the hooks.

| Setting | Value |
|---------|-------|
| **Profile tier** | <`lite` (8 core docs; the rest generated lazily when first needed) · `full` (all 20)> |
| **Auto-run permitted** | <yes · **no** (regulated change-management: human approval evidence required — auto-run is declined even when asked)> |
| **Plain-language layer** | <the language the 5W+1H plain layer is presented in, e.g. English · Bahasa Indonesia — engineer detail stays technical> |
| **Tracker** | <none · Jira · GitHub Issues> |
| **Comment allowlist** | <license header: yes/no · public-API doc-comments (JSDoc/docstrings): yes/no — default both NO (zero comments); "yes" is for legal/published-library requirements> |
| **Plugin version at setup/refresh** | <x.y.z — refresh compares this against the running plugin and offers migration for docs/sections added since> |

> **Shared state note:** `docs/basics/` is edited by every teammate's runs — on a merge conflict here,
> resolve by re-running refresh (reconcile), never by hand-picking hunks. Per-feature dirs are
> effectively single-writer.

## Profile index

> **Start here for how the app fits together:** [domain-model](./06-domain-model.md) (the shared entities) +
> [feature-map](./16-feature-map.md) (how features depend on each other). Then the rest as needed.

| Doc | Covers | Commit stamp |
|-----|--------|--------------|
| [architecture](./02-architecture.md) | structure, layering, module map | `<hash>` |
| [ui-architecture](./03-ui-architecture.md) | navigation, state, design system *(clients)* | `<hash>` |
| [ux-conventions](./04-ux-conventions.md) | interaction behavior — validation, states, snackbars, wizards *(clients)* | `<hash>` |
| [tech-stack](./05-tech-stack.md) | languages, frameworks, build/run/test | `<hash>` |
| [domain-model](./06-domain-model.md) | entities, relationships, cross-feature ownership | `<hash>` |
| [database](./07-database.md) | engine, schema shape, migrations | `<hash>` |
| [data-cache](./08-data-cache.md) | local persistence, caching, shared-state sync, offline | `<hash>` |
| [environment](./09-environment.md) | envs, config/secrets, full-stack run recipe, flags | `<hash>` |
| [conventions](./10-conventions.md) | code/naming/folder/test conventions | `<hash>` |
| [git-management](./11-git-management.md) | branching, commit/PR/merge, releases | `<hash>` |
| [security-compliance](./12-security-compliance.md) | auth, PII, encryption, compliance | `<hash>` |
| [auth](./13-auth.md) | runtime token handling — refresh, 401 | `<hash>` |
| [cicd-deployment](./14-cicd-deployment.md) | pipeline, release, rollback | `<hash>` |
| [api-reference](./15-api-reference.md) | base-URL matrix, machine-checkable contract, catalog | `<hash>` |
| [feature-map](./16-feature-map.md) | features, dependencies, entity ownership | `<hash>` |
| [asset-registry](./17-asset-registry.md) | registered assets — images, icons, font files *(clients)* | `<hash>` |
| [design-tokens](./18-design-tokens.md) | the visual contract — spacing, type ramp, fonts, colors, borders, sizing *(clients)* | `<hash>` |
| [code-inventory](./19-code-inventory.md) | every reusable code unit project-wide — helpers, base classes, wrappers; duplicates flagged | `<hash>` |
| [tech-debt-register](./20-tech-debt-register.md) | the debt ledger — open + paid, stable TD-IDs, ceilings | `<hash>` |

_Docs not applicable to this repo are omitted (see list below)._

**Skipped:** <docs skipped and why — e.g. "ui-architecture, asset-registry — backend repo, no UI">
