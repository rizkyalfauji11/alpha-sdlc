# Git Management — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> How this repo uses git. Versioning/deploy details live in [cicd-deployment](./cicd-deployment.md);
> this doc is the branching / commit / PR / merge side.

## Branching strategy

<Model — trunk-based / GitHub Flow / GitFlow. Long-lived branches (main, develop, release/*) and their roles. Branch naming (e.g. `feature/<ticket>-<slug>`, `hotfix/*`).>

## Protected branches

| Branch | Required checks | Required approvals | Who can merge |
|--------|-----------------|--------------------|---------------|
| <main> | <CI, lint> | <n> | |

## Commit conventions

<Message format — Conventional Commits? scope prefixes? Ticket key in message? Signed/verified commits (GPG)? Squash norms.>

## Pull requests

<Review requirements, min approvals, size norms, required CI checks, draft-PR usage, PR template location. Link the ticket ↔ branch ↔ PR relationship if standardized.>

## Merge strategy

<Squash / merge-commit / rebase — which and when. Delete branch on merge? Who performs the merge.>

## Branch ↔ environment mapping

<Which branch deploys to which environment (cross-ref [cicd-deployment](./cicd-deployment.md) for the pipeline side).>

## Tags & releases

<Tag scheme, release branches, changelog generation. Version numbers themselves → [cicd-deployment](./cicd-deployment.md).>

## Git hooks & tooling

<pre-commit / husky / lefthook, lint-staged, commitlint — what runs and where it's configured.>

## Ignore & large files

<`.gitignore` conventions, Git LFS usage. **Secrets are never committed** — see [security-compliance](./security-compliance.md).>

## Repo topology

<Monorepo / multi-repo / submodules, and this repo's relation to siblings.>
