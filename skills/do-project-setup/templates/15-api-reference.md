# API Reference — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> The project-wide API topology — kills "which base URL for which service in which env"
> confusion. **No tokens/keys here** — auth *method* only. In multi-repo, this map is
> authoritative in one repo (contract/backend); other repos reference it.

## Base-URL matrix

| Service / API | dev | staging | prod | Auth method | Owner (team/repo) | Defined in |
|---------------|-----|---------|------|-------------|-------------------|-----------|
| <Balance service> | | | | Bearer token | | <config file> |
| <QRIS service> | | | | mTLS | | |

## Machine-checkable contract

| Contract | Spec file / link | Owner repo | Clients derive (typed client / fixtures)? | Covers all live endpoints? |
|----------|------------------|------------|-------------------------------------------|-----------------------------|
| <service API> | <path to OpenAPI/Swagger, or "prose-only — drift risk"> | | <e.g. openapi-typescript → `src/api/types.ts`, or "hand-written"> | <yes / GAPS: list unspecced endpoints> |

> The authoritative shape both sides agree on. If a contract is prose-only, flag it — that's where
> method/path/field-shape drift (405s, object-rendered-as-string) comes from. **An endpoint that exists
> in code but not in the spec is invisible drift** — the client can't derive types for it and hand-rolls
> them; flag every unspecced endpoint as a gap (same discipline as domain-model contradictions).

## API catalog

| API | Consumed / Exposed | Version | Purpose | Docs (OpenAPI/Swagger) |
|-----|--------------------|---------|---------|------------------------|
| <name> | consumed | v2 | | <link> |

## Conventions

<Pagination, timeouts, retry/backoff policy, rate limits.>

**Standard error envelope** — the one shape every error response uses; client error-handling
([conventions](./10-conventions.md)) and error-state UX ([ux-conventions](./04-ux-conventions.md)) map from it:

```json
{ "<code>": "<machine-readable error code>", "<message>": "<human-readable>", "<details>": "<field-level errors, optional>" }
```

<Fields, which are always present, and where the envelope is defined in the spec.>

**Breaking-change policy:** <how contract changes roll out across consumers — additive-only? deprecation window + sunset? versioned paths? — so an interdependent consumer is never surprised by a removed/renamed field.>

## Gotchas

<The weird ones a table can't hold — legacy hosts, per-service auth quirks, v1/v2 coexistence, env-specific oddities.>
