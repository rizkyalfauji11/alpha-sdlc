# Tech Stack — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> Versions are **not copied here** — they live in the manifest. This lists what's used and why.

## Languages & frameworks

| Layer | Tech | Notes |
|-------|------|-------|
| Language | <e.g. Kotlin / Swift / TypeScript> | |
| Framework | <e.g. Compose / SwiftUI / Next.js> | |
| Runtime / min version | <e.g. minSdk 24 / iOS 15 / Node 20> | |

## Key libraries

<The handful that shape how code is written — DI, networking, async, persistence, testing. Purpose each. **Versions → the manifest** (`package.json` / `build.gradle` / `Podfile` / `go.mod`).>

## Build tooling

<Build system (Gradle / SPM / Vite / Bazel), package manager, code-gen if any.>

## Build / run / test

```
# Build
<command>
# Run locally
<command>
# Test
<command>
```

<Prerequisites — SDKs, tool versions, env setup.>
