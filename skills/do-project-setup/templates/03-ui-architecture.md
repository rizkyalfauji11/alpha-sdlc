# UI Architecture — <project name>  *(client repos only)*

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

## Platform & UI toolkit

<e.g. Android — Jetpack Compose / XML Views; iOS — SwiftUI / UIKit; Web — React/Vue + framework.>

## Navigation

<How navigation works — nav graph / router / coordinator — and the top-level flow.>

## State management (UI / client state)

<Approach for **UI state** — e.g. ViewModel + StateFlow, Redux, MVI, hooks/context. Where state lives, how it flows. **Server state (remote data, caching, cross-feature sync) is NOT here** — that's [data-cache](./08-data-cache.md) → *Shared server-state sync*; keep the two distinct so "which layer keeps this list fresh" is never ambiguous.>

## Design system & component inventory

<The component library / design system, theming, and where reusable UI components live. Interaction *behavior* conventions → [ux-conventions](./04-ux-conventions.md).>

> **Search here before building any UI component** (same discipline as the asset registry): if the
> design shows a button/card/dialog/stepper, reuse the canonical component below — a hand-rolled
> look-alike is how the built UI drifts from the design. New reusable components are registered here
> on create.

| Component | Path / package | Variants & states supported | Notes |
|-----------|----------------|-----------------------------|-------|
| <AppButton> | <src/components/AppButton.tsx> | <primary/secondary/danger · disabled/loading> | |
| <AppDialog> | <…> | <sizes · scrollable body> | fits-or-scrolls, never clips |

## Where assets live

<Asset directories per platform (Android `res/drawable`·`mipmap`, iOS asset catalogs, web `/assets`) and the design-system/assets module. This is the map the asset search flow uses — see [asset-registry](./17-asset-registry.md).>

## Screen inventory

> Group the screens that form a **multi-step flow** (wizard) under one flow name — they're one journey,
> not isolated screens (stepped-flow behavior → [ux-conventions](./04-ux-conventions.md) → *Multi-step / wizard flows*).

| Screen | Route / entry | Flow (if part of one) | Notes |
|--------|---------------|-----------------------|-------|
| <name> | <route> | <e.g. flow-name · step 2 of 3> | |
