# Tech-Debt Register — <project name>

_Generated by `do-project-setup` · commit `<hash>` · approved <YYYY-MM-DD>_

> **The single ledger of every known debt — open and history.** Debt is born all over the pipeline
> (a named simplification with its ceiling, a contradiction two docs flagged, a duplicate
> implementation, a deferred migration, a foundation omission, a class-suspicion a fix recorded) —
> this is where every one of them **also** lands, so nothing rots in a table nobody re-reads.
> `do-tech-debt-grooming` reads this as its **backlog**; the end-of-feature reconcile checks for
> new entries; **paid rows are kept** (struck through, with their resolution) — the register is the
> history too. IDs are stable (`TD-<n>`), never renumbered.

## Open

| ID | What (plain · engineer) | Kind | Origin (where/when it was born) | Ceiling — when it bites | Status |
|----|-------------------------|------|----------------------------------|--------------------------|--------|
| TD-1 | <two date formatters that drift apart · `feature-a/DateUtil` vs `feature-b/date.ts`> | duplicate | <`19-code-inventory` Duplicates · <date>> | <locale bugs when either changes> | open |
| TD-2 | <body text at 13/14/15sp across screens · token contradiction> | contradiction | <`18-design-tokens` Contradictions · <date>> | <every new screen guesses again> | groomed → <TRD link> |
| TD-3 | <single-region deploy · no replication> | named simplification | <TRD <feature> §<n> · <date>> | <traffic crosses <X>> | accepted — <why, by whom, revisit when> |

- **Kind:** named simplification (with ceiling) · contradiction (docs/code disagree) · duplicate implementation · deferred migration · omission (foundation/base) · class-suspicion (from a fix, unproven).
- **Status:** `open` · `groomed → <TRD link>` (a tech-debt TRD exists) · `accepted — <why + revisit trigger>` (a deliberate no-fix, never a silent one) · paid rows move below.

## Paid (history)

| ID | What | Resolved by | Date |
|----|------|-------------|------|
| ~~TD-0~~ | <extracted the token file the project lacked> | <tech-debt TRD <link> · shipped <commit>> | <date> |

## Where debt is born → it must land here too (register-on-create)

<`18-design-tokens` + `06-domain-model` *Contradictions* · `19-code-inventory` *Duplicates* · foundation TRD *Deliberate omissions* · any TRD/stage's **named simplification** · `do-fixing`'s recorded class-suspicions and sibling-screen findings · auto-run's deferred recommendations. Those tables keep the domain detail; this register keeps the ledger view — ID, status, and the ceiling. On refresh, reconcile: an entry in any source table with no `TD-<n>` row is a gap.>
