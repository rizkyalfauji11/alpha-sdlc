# Security & Compliance — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> ⚠️ **Observed controls only.** Do **not** assert a compliance status that isn't verified.
> Anything unconfirmed is a **GAP** for security/compliance to fill. **No secret values here.**
> **Requires human sign-off before this doc is trusted** — see bottom.

## Authentication & authorization

<How users/services authenticate (tokens, OAuth, mTLS, session), and how authorization is enforced (roles, scopes). Mark `UNKNOWN` where undetermined.>

## Data classification & PII

<What sensitive/PII data the system handles, how it's classified, and handling rules (masking, minimization, retention).>

## Encryption

| Aspect | Control observed | Gap? |
|--------|------------------|------|
| In transit | <TLS version, cert pinning?> | |
| At rest | <DB/disk encryption, keystore/keychain> | |

## Compliance regimes

<Applicable regimes (e.g. OJK, PCI-DSS, GDPR, data residency). **State only what is confirmed by a human**; otherwise "GAP — needs compliance confirmation".>

## Secrets management

<Where secrets live and how they're accessed (vault, secret manager, CI). Names/locations only.>

## Security testing

<VAPT / SAST / DAST / dependency scanning in place, cadence, where results live.>

## Audit logging

<What's logged for audit, where, retention.>

## Gaps (needs human input)

- <unconfirmed item → who should confirm>

## Sign-off

- **Reviewed by:** <name / security function> · **Date:** <YYYY-MM-DD>
