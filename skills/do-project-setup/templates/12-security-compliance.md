# Security & Compliance — <project name>

_Generated by `do-project-setup` · commit `<hash>` · approved <YYYY-MM-DD>_

> ⚠️ **Observed controls only.** Do **not** assert a compliance status that isn't verified.
> Anything unconfirmed is a **GAP** for security/compliance to fill. **No secret values here.**
> **Requires human sign-off before this doc is trusted** — see bottom; `do-development`'s
> profile-conformance
> review reads that line, and while it is unsigned every PII/secret conformance check against this
> doc is a
> **recorded GAP** named in the step report, never a silent trust.

## Authentication & authorization

<How users/services authenticate (tokens, OAuth, mTLS, session), and how authorization is enforced
(roles, scopes). Mark `UNKNOWN` where undetermined. **Policy & controls live here; runtime token
mechanics (refresh flow, 401 handling, storage) → [auth](./13-auth.md)** — keep the two in sync.>

**Roles × capabilities** — who may do what, per feature area. This is what grooming derives
authorization AC from (e.g. "role X cannot delete a shared entity"); a vague authz rule is how
missing enforcement ships.

| Role | <feature area 1> | <feature area 2> | Notes |
|------|------------------|------------------|-------|
| <admin> | <full> | <full> | |
| <staff> | <read + edit, no delete> | <read> | |

## Trust boundaries & input validation

<**Client validation is UX; server validation is security.** Every endpoint validates its input
independently, regardless of what the client already checked — the classic vulnerability is
validating only in the form. Record where server-side validation lives (middleware/schema layer) and
any endpoint exempt (there should be none). Mark `UNKNOWN` if undetermined.>

## Data classification & PII

<What sensitive/PII data the system handles, how it's classified, and handling rules (masking,
minimization, retention).>

## Encryption

| Aspect | Control observed | Gap? |
|--------|------------------|------|
| In transit | <TLS version, cert pinning?> | |
| At rest | <DB/disk encryption, keystore/keychain> | |

## Compliance regimes

<Applicable regimes (e.g. OJK, PCI-DSS, GDPR, data residency). **State only what is confirmed by a
human**; otherwise "GAP — needs compliance confirmation".>

## Secrets management

<Where secrets live and how they're accessed (vault, secret manager, CI). Names/locations only.>

## Security testing

<VAPT / SAST / DAST / dependency scanning in place, cadence, where results live.>

## Audit logging

<What's logged for audit, where, retention.>

## Gaps (needs human input)

> Each gap stays here until a human closes it, and each is also raised as a question naming who
> must answer. Candidates come only from this repository: the authors of the lines that raised the
> gap (git history), and whoever signed this doc before. Names only, never an email. No candidate
> readable → `UNKNOWN — needs human input`, said plainly, never guessed.

| Gap | Raised by (file:line) | Candidates (name · role · prior sign-off) | Where the names came from |
|-----|-----------------------|-------------------------------------------|---------------------------|
| <e.g. how long audit logs are kept> | <`internal/audit/log.go:42`> | <name · role · "signed 12 before" — or `UNKNOWN — needs human input`> | <e.g. git blame of the three log calls> |

## Sign-off

> Until this line is filled by the person who signs, the doc sits at *needs sign-off*; the rest of
> the profile carries on without it. This line is also where the next setup reads a prior sign-off
> from, when it proposes candidates.

- **Reviewed by:** <name / security function> · **Date:** <YYYY-MM-DD>
