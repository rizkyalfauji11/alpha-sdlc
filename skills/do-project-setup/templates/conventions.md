# Conventions — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> The "match the codebase" reference. Complements `CLAUDE.md` (short how-we-work instructions).

## Code style & linting

<Formatter/linter (ktlint, SwiftLint, ESLint/Prettier, detekt), key rules, how it's enforced (CI, pre-commit).>

## Naming

<Naming conventions for files, classes, functions, variables, and resources.>

## Folder / module structure

<The *rules* for organizing code — feature-first vs layer-first, where new code goes, when to add a module. The actual directory tree lives in [architecture](./architecture.md) → *Code structure*; don't duplicate it here.>

## Testing conventions

<Test frameworks, where tests live, naming, unit vs integration split, coverage expectations, TDD practice if any.>

## Git / PR conventions

See [git-management](./git-management.md) — branching, commit/PR/merge conventions live there.
