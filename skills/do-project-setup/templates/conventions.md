# Conventions — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> The "match the codebase" reference. Complements `CLAUDE.md` (short how-we-work instructions).

## Code style & linting

<Formatter/linter (ktlint, SwiftLint, ESLint/Prettier, detekt), key rules, how it's enforced (CI, pre-commit).>

## Naming

<Naming conventions for files, classes, functions, variables, and resources.>

## Folder / module structure

<The *rules* for organizing code — feature-first vs layer-first, where new code goes, when to add a module. The actual directory tree lives in [architecture](./architecture.md) → *Code structure*; don't duplicate it here.>

## Data contract & localized rendering

> How the app consumes backend data safely — the conventions that stop the "FE assumed a shape the
> BE doesn't send" class of runtime crash. Fill from the real code; mark `UNKNOWN` if not established.

- **Typed client from the contract:** <does the FE generate its API types/client from the OpenAPI spec (openapi-typescript, orval, etc.), or hand-write them? Generated is the goal — a shape mismatch then fails at compile time. Name the tool + command, or flag hand-written types as a drift risk.>
- **Localized data fields:** <backend fields that are localized objects (e.g. `{ en, id }`) — these must render through a single locale helper (`t()` / `localize()`), **never raw** in JSX/markup (raw render = "Objects are not valid as a React child"). Name the helper and any lint rule enforcing it.>
- **Fixtures:** <where test fixtures/mocks come from — generated from the contract or recorded real responses (safe), vs. hand-authored (drifts). State the rule so isolated tests can't pass on a shape reality never sends.>

## Testing conventions

<Test frameworks, where tests live, naming, unit vs integration split, coverage expectations, TDD practice if any.>

## Git / PR conventions

See [git-management](./git-management.md) — branching, commit/PR/merge conventions live there.
