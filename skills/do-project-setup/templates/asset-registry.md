# Asset Registry — <project name>  *(client repos only)*

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> Searchable inventory of registered assets — **search here before creating a new one**
> (the asset search flow: registry → assets module → ask user → create). New assets are
> **registered on create** (`do-development`). Tokens/pixels live in Figma, not here.

## Registered assets

| Asset name | What it is | Type | Path(s) per platform | Tags / keywords |
|------------|-----------|------|----------------------|-----------------|
| `ic_scan_qris` | QRIS scan icon, filled | icon | Android `res/drawable/…` · iOS catalog · web `/assets/…` | scan, qris, camera, pay |

## Naming conventions

<The naming scheme for assets — so search-by-name works and new assets are consistent.>

## Where assets live

<Asset directories per platform (mirrors [ui-architecture](./ui-architecture.md)).>

## Icon set / library

<Material Icons / SF Symbols / custom set — reuse from here before creating.>

## Design tokens

<Colors, typography, spacing — **source of truth is Figma / the tokens file** (linked). Cache token *names* here, not values.>

## Adding an asset

<How a new asset gets in — Figma export? manual? optimization/density rules — and the reminder to register it here on create.>
