# Database — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> Schema *shape* only — **full DDL lives in the migration files** (linked below), not copied here.

## Engine(s)

<e.g. PostgreSQL 15, Redis (cache), plus any read replicas / sharding. Mark `UNKNOWN` if undetermined.>

## Schema shape

<The core entities and relationships — the overview, not every column.>

```mermaid
erDiagram
  ENTITY ||--o{ CHILD : has
  ENTITY {
    id pk
    key_field type
  }
```

## Migrations

<Migration tool/approach (Flyway, Liquibase, Room, Prisma, etc.), where migrations live, how they run, ordering rules. **Link the migrations directory.**>

## Notes

<Indexing/performance conventions, soft-delete, multi-tenancy, data retention — anything a schema change must respect.>
