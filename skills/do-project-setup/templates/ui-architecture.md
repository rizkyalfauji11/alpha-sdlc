# UI Architecture — <project name>  *(client repos only)*

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

## Platform & UI toolkit

<e.g. Android — Jetpack Compose / XML Views; iOS — SwiftUI / UIKit; Web — React/Vue + framework.>

## Navigation

<How navigation works — nav graph / router / coordinator — and the top-level flow.>

## State management

<Approach — e.g. ViewModel + StateFlow, Redux, MVI, hooks/context. Where state lives, how it flows.>

## Design system & components

<The component library / design system, theming, and where reusable UI components live.>

## Where assets live

<Asset directories per platform (Android `res/drawable`·`mipmap`, iOS asset catalogs, web `/assets`) and the design-system/assets module. This is the map the asset search flow uses — see [asset-registry](./asset-registry.md).>

## Screen inventory

| Screen | Route / entry | Notes |
|--------|---------------|-------|
| <name> | <route> | |
