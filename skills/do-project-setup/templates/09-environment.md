# Environments & Config — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> **No secret values here** — record the *name* and *where it lives*, never the value.

## Environments

| Env | Purpose | Third-party services (sandbox / live) | Notes |
|-----|---------|----------------------------------------|-------|
| dev | | <e.g. payments sandbox · email mocked> | |
| staging | | <sandbox> | |
| prod | | <live> | |

> Record **which external services are sandbox vs live per env** — so a test or smoke run never
> accidentally hits a live third party.

## Config & secrets approach

<How config is provided (env vars, `.env`, remote config, build config), where secrets come from (vault, secret manager, CI secrets), and how they reach the app. Link the config files.>

## Required config / env vars

> **Behavior if missing** is part of the contract: a required var that **silently defaults** (an API
> base URL falling back to localhost) is the classic "boots fine, then 404/CORS at runtime" bug.
> Convention: required config **fails fast at startup** with a clear message — never a silent default.

| Name | Purpose | Required? | If missing (fail-fast / default) | Source (file / manager) |
|------|---------|-----------|----------------------------------|-------------------------|
| <VAR_NAME> | | <required> | <fail-fast at startup> | |

## Full-stack run recipe

> How to boot the **assembled application the way it actually runs** — so `do-development` and
> `do-testing` can stand up the real frontend + real backend together for the integrated Boot & Smoke
> gate. Fill one row per service. For multi-repo, note the repo/path. **No secret values.**

| Service (repo) | Start command | Ready-check (health URL / log line) | Default port |
|----------------|---------------|-------------------------------------|--------------|
| <backend> | <e.g. `./gradlew bootRun`, `npm run dev`> | <e.g. `GET /healthz` 200> | |
| <frontend> | <e.g. `npm run dev`> | <e.g. `http://localhost:3001` 200> | |

**Frontend → backend wiring:** <which env var / config points the FE at the running BE, per env — e.g. `VITE_API_BASE_URL` in `.env.development`. This is the seam that produces 405 / CORS / base-URL bugs when wrong.>

**CORS / origins:** <the origins the backend must allow for the frontend to call it in dev, and where that's configured.>

**One-command boot (if any):** <docker-compose / Makefile / Procfile target that brings the whole stack up, if one exists — else "none; start services individually per the table.">

**Seed step:** <after boot, load domain-realistic data per [database](./07-database.md) → *Seed / test data* — boot → seed → ready. Boot & Smoke and the data-flow tests assume a seeded stack, not an empty one.>

## Feature flags

<Flag system, where flags are defined, naming.>

## Build variants / flavors

<Debug/release, product flavors, schemes, build targets — and what differs between them.>
