# Tech Stack — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> Versions are **not copied here** — they live in the manifest. This lists what's used and why.

## Languages & frameworks

| Layer | Tech | Notes |
|-------|------|-------|
| Language | <e.g. Kotlin / Swift / TypeScript> | |
| Framework | <e.g. Compose / SwiftUI / Next.js> | |
| Runtime / min version | <e.g. minSdk 24 / iOS 15 / Node 20> | |

## Key libraries — the approved library per concern

> The **ladder-rung-5 lookup**: before adding any dependency, find the concern here and use the
> library the project already has — a second library for a covered concern (axios beside the fetch
> wrapper, moment beside date-fns) is drift, not a choice. If a concern is missing, adding it is a
> gated decision — then register it here. **Versions → the manifest** (`package.json` / `build.gradle` / `Podfile` / `go.mod`).

| Concern | Approved library / mechanism | Notes |
|---------|------------------------------|-------|
| <Networking / HTTP> | <e.g. the `apiClient` wrapper (fetch)> | <never call fetch/axios directly> |
| <Server state / caching> | <e.g. React Query> | <sync rules → [data-cache](./08-data-cache.md)> |
| <Forms & validation> | <e.g. react-hook-form + zod> | |
| <Dates/time> | <e.g. date-fns> | |
| <DI / async / persistence / testing…> | | |

## Code generation

<Generated code and how to regenerate it — most importantly the **typed API client** generated from the machine-checkable contract ([api-reference](./15-api-reference.md)): the tool (openapi-typescript / orval / …), the command, where output lands, and when to re-run (any contract change). Plus any other codegen (DB models, GraphQL, protobuf).>

```
# Regenerate typed API client
<command>
```

## Build tooling

<Build system (Gradle / SPM / Vite / Bazel), package manager.>

## Build / run / test

> **This repo's** commands. Booting the whole stack (multiple services together) →
> [environment](./09-environment.md) → *Full-stack run recipe*.

```
# Build
<command>
# Run locally
<command>
# Test
<command>
```

<Prerequisites — SDKs, tool versions, env setup.>
