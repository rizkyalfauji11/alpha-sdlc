# CI/CD & Deployment — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

> Pipeline *shape* — **exact config lives in the pipeline files** (linked), not copied here.

## CI/CD tool

<e.g. GitHub Actions / GitLab CI / Jenkins / Bitrise / ArgoCD. Link the pipeline files.>

## Pipeline stages

| Stage | Runs | Trigger |
|-------|------|---------|
| Build | | on PR / merge |
| Test | <which suites at this stage — e.g. PR: unit + lint · merge: API + integration · nightly/pre-release: E2E> | |
| Deploy | | |

> **Map each test suite to the stage that runs it** (unit / API / UI / integration / E2E). A suite no CI
> stage runs is only enforced when someone remembers to run it locally — say where each one gates.

```mermaid
graph LR
  PR --> Build --> Test --> Deploy
```

## Environment promotion

<How code moves dev → staging → prod (auto/manual gates, approvals).>

## Release process

<For services: deploy strategy (rolling/blue-green/canary). For mobile: store submission, phased rollout, forced-update, feature-flag gating.>

**Migrations on deploy:** <how schema migrations run at release — automatic in the pipeline vs manual gate, their order relative to the code rollout (e.g. expand → migrate → contract for zero-downtime), and the rollback implication (see Rollback). Migrations themselves → [database](./07-database.md).>

## Versioning

<Version scheme, tagging, build numbers.>

## Rollback

<How to revert a bad release — revert commit, redeploy previous, flag off, DB rollback constraints.>
