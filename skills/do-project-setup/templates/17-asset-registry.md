# Asset Registry — <project name>  *(client repos only)*

_Generated by `do-project-setup` · commit `<hash>` · approved <YYYY-MM-DD>_

> Searchable inventory of registered assets — **search here before creating a new one**
> (the asset search flow: registry → assets module → ask user → create). New assets are
> **registered on create** (`do-development`).
> **Division:** images / icons / **font files** → this registry; reusable **UI components** →
> [ui-architecture](./03-ui-architecture.md) → component inventory; **design token names *and* values**
> (spacing, typography, color, border, radius, sizing) → [design-tokens](./18-design-tokens.md).

## Registered assets

| Asset name | What it is | Type | Path(s) per platform | Variants (dark · densities) | Tags / keywords |
|------------|-----------|------|----------------------|------------------------------|-----------------|
| <`ic_search`> | <search icon, outlined> | icon | <Android `res/drawable/…` · iOS catalog · web `/assets/…`> | <light+dark · all densities> | <search, find, magnifier> |

> **Variants matter:** a light-only icon reused on a dark theme is the "icon invisible in dark mode"
> bug — record whether each asset has dark/density variants so reuse picks a complete one.

## Naming conventions

<The naming scheme for assets — so search-by-name works and new assets are consistent.>

## Where assets live

<Asset directories per platform (mirrors [ui-architecture](./03-ui-architecture.md)).>

## Icon set / library

<Material Icons / SF Symbols / custom set — reuse from here before creating.>

## Design tokens

**Not here → [18-design-tokens.md](./18-design-tokens.md)**, which mirrors token names **and values** (per
platform) because a build agent can't read Figma mid-stage. Figma / the design library stays the upstream
source. This registry only holds the **font files** those typography tokens point at (above).

## Adding an asset

<How a new asset gets in — Figma export? manual? optimization/density rules — and the reminder to register it here on create.>
