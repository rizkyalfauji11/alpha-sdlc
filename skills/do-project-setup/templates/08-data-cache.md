# Local Data & Cache — <project name>

_Generated by `do-project-setup` · commit `<hash>` · <YYYY-MM-DD>_

## Local persistence

| Store | Tech | What's stored | Notes |
|-------|------|---------------|-------|
| <e.g. local DB> | Room / CoreData / IndexedDB | | |
| <e.g. key-value> | DataStore / UserDefaults / localStorage | | |
| <flow drafts> | <where in-progress multi-step/wizard state is persisted, if the app has stepped flows> | <draft of a half-completed flow> | partial save/resume rules → [ux-conventions](./04-ux-conventions.md) → *Multi-step / wizard flows* |

## Caching strategy

<In-memory / disk caches, what's cached, invalidation, TTLs, and the rate-limit/refresh policy for remote data.>

## Shared server-state sync

> **How shared data stays consistent across features/screens** — the convention that prevents "the menu
> list in recipe is stale / not synchronized." Fill from the real code; if there's no convention yet,
> establish one with the user (this is a common bug source when features share data).

- **Server-state library:** <e.g. React Query / SWR / RTK Query — the single mechanism for remote data>
- **Query-key convention:** <how keys are structured so every consumer of an entity reads the *same* cache entry — e.g. `['menus']`, `['menu', id]`. One entity → one canonical key, not per-feature copies.>
- **Mutation → invalidation map:** which consumers refetch when an entity changes, so a change in the owner is seen everywhere:

  | Mutation (owner feature) | Invalidates / refetches |
  |--------------------------|-------------------------|
  | <create/update/delete of an entity> | <its list key + any per-id keys — every consuming feature's view refetches> |

- **Real-time / push → cache update map** *(if the app receives server events — websocket / SSE / push)*: local mutations aren't the only way data changes — another user, another tab, or the server can change it. Map each server event to the cache entries it invalidates or patches, or a consumer's list only refreshes on *local* edits and goes stale on everyone else's:

  | Server event | Invalidates / patches |
  |--------------|------------------------|
  | <`<entity>.updated`> | <invalidate `['<entity>s']` / patch `['<entity>', id]`> |

- **Single source of truth:** a consuming feature reads shared entities from the **owner's** query/endpoint (per `06-domain-model.md`), never a private copy.
- **Staleness / refetch:** <staleTime, refetch-on-focus/reconnect, polling if any.>

## Offline behavior

<What works offline, how stale data is shown, sync/conflict handling on reconnect.>

## Sensitive data at rest

<What local data is sensitive and how it's protected (encrypted store, keystore/keychain). Never store secrets in plain local storage — flag if found. See [security-compliance](./12-security-compliance.md).>
