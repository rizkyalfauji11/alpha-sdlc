# Regression Coverage Map — <test project name>

_Generated by `do-regression-setup` · test-project commit `<hash>` · app-profile commit `<hash>` · <YYYY-MM-DD>_

> **The QA counterpart of the app's `16-feature-map.md`** — which features and flows have regression
> protection, and which are gaps. One row per shipped feature (seeded from the app's feature map);
> flows/bindings come from each feature's TRD (§2 flow dependencies, integrity AC, multi-step flows).
> **Register-on-create:** when a feature ships (app `do-testing` green), its rows are added here —
> a shipped feature with no row is a reconcile failure, a row with no test is a visible **GAP**.

## Coverage

| Feature (app feature-map) | Flow / binding to protect | Kind | Test (path) | Suite | Status |
|---------------------------|---------------------------|------|-------------|-------|--------|
| <feature-1> | <critical journey: create → list → edit> | journey | <specs/…> | `@critical` | covered |
| <feature-3> | <options dropdown ← feature-1 (create direction, freshness)> | flow-binding | <specs/…> | `@regression` | covered |
| <feature-3> | <owner delete → consumer shows "unavailable" (destructive)> | integrity | — | — | **GAP** |
| <feature-2> | <wizard: per-step validation · abort-clean · atomic commit> | stepped-flow | <specs/…> | `@regression` | covered |

- **Kind:** journey · flow-binding (create/freshness) · integrity (destructive/visibility) · stepped-flow · contract/API.
- Each test names the **condition user(s)** it needs (from `05` → Test user catalog); a row whose condition has no catalog user is blocked on adding the user + seed, never on improvised state.
- **Status:** covered · planned (issue link) · **GAP** (loud, never silent) · quarantined (→ `08`, counts as a gap).

## Gaps summary

<The current GAP + quarantined rows in one list — what ships unprotected today. This is the first thing
the reconcile checks and the last thing a release review reads.>

## Reconcile rule

<On every refresh (and at each feature's SDLC completion): diff this map against the app's
`16-feature-map.md` + new TRDs — every new/changed feature and every new flow binding gets a row
(covered, planned, or GAP). Rows never disappear silently; a removed feature's rows are retired with a note.>
