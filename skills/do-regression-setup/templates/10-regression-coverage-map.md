# Regression Coverage Map — <test project name>

_Generated by `do-regression-setup` · test-project commit `<hash>` · app build explored `<version/build>` · <YYYY-MM-DD>_

> Which features and flows of the app have regression protection, and which are gaps. One row per
> feature/flow in the **learned model** (`04-app-under-test.md` — from the PRD + exploration).
> **Register-on-create:** when the app ships something new (new build explored, new PRD provided),
> its rows are added here — a known feature with no row is a reconcile failure; a row with no test
> is a visible **GAP**, never silent.

## Coverage

| Feature (learned model) | Flow / condition to protect | Kind | Test (path) | Suite | Status |
|-------------------------|------------------------------|------|-------------|-------|--------|
| <feature-1> | <critical journey: create → list → edit> | journey | <specs/…> | `@critical` | covered |
| <feature-3> | <consumer list reflects source creation (freshness)> | flow-binding | <specs/…> | `@regression` | covered |
| <feature-3> | <owner archive → consumer shows "unavailable" (destructive)> | integrity | — | — | **GAP** |
| <feature-2> | <wizard: per-step validation · abort-clean · atomic commit> | stepped-flow | <specs/…> | `@regression` | covered |

- **Kind:** journey · flow-binding (create/freshness) · integrity (destructive/visibility) · stepped-flow · contract/API · visual (needs provided designs).
- Each test names the **condition user(s)** it needs (`05` → Test user catalog); a row whose condition has no catalog user is blocked on adding the user + seed, never on improvised state.

## Gaps summary

<The current GAP + quarantined rows in one list — what runs unprotected today. First thing the refresh checks, last thing a release review reads.>

## Reconcile rule

<On every refresh: diff this map against the current learned model (`04`) — every new/changed screen, flow, or PRD feature gets a row (covered · planned · **GAP**). Rows never disappear silently; a removed feature's rows are retired with a note.>
