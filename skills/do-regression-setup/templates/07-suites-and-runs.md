# Suites & Runs — <test project name>

_Generated by `do-regression-setup` · test-project commit `<hash>` · <YYYY-MM-DD>_

## Suite taxonomy

| Suite (tag) | Scope | Entry criteria | Runtime budget |
|-------------|-------|----------------|----------------|
| <`@smoke`> | <app boots, auth works, each feature's entry screen renders> | <every test green & stable> | <≤ N min> |
| <`@critical`> | <the money paths — critical journeys + destructive/freshness bindings> | | <≤ N min> |
| <`@regression`> | <full coverage map (10)> | | <≤ N min> |

<Tagging convention: where tags live, one primary suite tag per test, feature tags (`@<feature>`) so a feature's tests can run alone.>

## Run matrix — what runs when

| Trigger | Suite | Target env | Gate? |
|---------|-------|-----------|-------|
| <app PR merge> | <smoke> | <dev/ephemeral> | <blocks deploy?> |
| <nightly> | <full regression> | <staging> | <report> |
| <pre-release> | <critical + regression> | <staging> | <blocks release> |
| <post-deploy prod> | <smoke> | <prod (read-only paths!)> | <alert> |

## CI wiring

<Where the suite's pipeline lives (this repo), how it's triggered by the app's pipeline (per the app's `14-cicd-deployment.md`), and where each run's results land (→ `09-reporting-and-triage.md`). A suite no trigger runs protects nothing — every suite tag appears in the matrix.>

## Ownership

<Who watches which run, and the response expectation when a gate goes red.>
