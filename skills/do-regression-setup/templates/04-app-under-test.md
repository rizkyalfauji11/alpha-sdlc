# App Under Test — <test project name>

_Generated by `do-regression-setup` · test-project commit `<hash>` · app-profile commit `<hash>` · <YYYY-MM-DD>_

> **The bridge.** The app's `docs/basics/` is the single source of truth — this doc *points* at it,
> and QA never re-declares an app truth (a copied truth drifts). If a link below is stale (app moved
> past the stamped commit), refresh this profile.

## Application repos & platforms

| Platform | Repo / path | App profile (`docs/basics/`) | Notes |
|----------|-------------|------------------------------|-------|
| <Backend> | <path/url> | <link> | |
| <Web> | <path/url> | <link> | |

## The app truths this suite consumes

| Truth | Where it lives (app) | Used for |
|-------|----------------------|----------|
| Domain model (entities · ownership · lifecycle · on-delete) | <`06-domain-model.md`> | integrity assertions |
| Feature map (features · dependencies · flow bindings) | <`16-feature-map.md`> + each feature's TRD §2 | the coverage map (10) |
| API contract (machine-checkable) + typed client | <`15-api-reference.md` → spec path> | API tests, fixtures |
| Widget-spec Test IDs | <`docs/development/<feature>/widget-spec/`> | all UI locators (→ 06) |
| Test/dev authentication | <`13-auth.md` → Test / dev authentication> | test accounts (→ 05) |
| Full-stack run recipe + seed | <`09-environment.md`, `07-database.md` → Seed> | booting/seeding a target (→ 05) |
| UX conventions & scaffolds | <`04-ux-conventions.md`, `03-ui-architecture.md`> | conformance assertions |

## Built artifact — what the suite tests and how it gets it

> The suite tests the **built application**, black-box — never app source.

| Platform | Artifact | Obtained via | Install / target step |
|----------|----------|--------------|------------------------|
| <Web> | <deployed URL per env> | <env config (→ 05)> | <none — navigate> |
| <Android> | <APK/AAB build> | <download from the app's CI (per its `14-cicd-deployment.md`), or local build command> | <install on emulator/device> |
| <iOS> | <.app/.ipa> | <same> | <install on simulator/device> |

## Version / build under test

<Build/version pinning: which app commit/version this run certifies, which deployed env maps to which build, compatibility rule (suite version vs app version).>
