# App Under Test — <test project name>

_Generated by `do-regression-setup` · test-project commit `<hash>` · app build explored `<version/build>` · <YYYY-MM-DD>_

> **The learned model of the application** — built from the user-provided inputs (PRD, docs, links,
> artifacts) and from **exploring the built app itself**. This project has no access to the app's
> repo or code, so this doc IS the source of truth the suite grounds in. **Every truth is tagged
> with its source** — `provided: <input>` · `learned: exploration` · `confirmed by user` ·
> `UNKNOWN — needs input` — and learned-not-confirmed truths are flagged, never silently trusted.

## Platforms & built artifacts

| Platform | Artifact | Obtained via | Install / target step |
|----------|----------|--------------|------------------------|
| <Web> | <deployed URL per env (→ 05)> | <provided link> | <none — navigate> |
| <Android> | <APK/AAB> | <provided file / download location> | <install on emulator/device> |
| <iOS> | <.app/.ipa> | <provided> | <install on simulator/device> |

**Build pinning:** <which app version/build this profile explored and each run certifies; how a new build is detected (provided notification, or manual).>

## Screen & flow map (learned)

<The app as explored — every reachable screen, its navigation, and the user flows end to end. Unreached areas are listed under Gaps, never implied covered.>

```mermaid
graph TD
  Login --> Home --> <ScreenA> --> <ScreenB>
```

| Screen | Reached via | Locator attributes present? | States observed | Source |
|--------|-------------|------------------------------|-----------------|--------|
| <name> | <nav path> | <data-testid ✓ / partial / none> | <empty · error · loading> | learned: exploration |

## Features & behavior rules

<The feature list and the behavior rules the suite protects — from PRD where provided, exploration otherwise. Include entity lifecycle rules (what states exist, who sees what), delete/archive behavior, and cross-feature flows (a list in one area fed by creations in another). Contradictions between PRD and the observed app are findings — record them.>

| Feature / rule | Statement | Source |
|----------------|-----------|--------|
| <feature-1> | <what it does / must do> | provided: PRD §<n> |
| <lifecycle rule> | <e.g. archived items don't appear in pickers> | learned: exploration — **confirm** |

## API surface

<From the provided Swagger/OpenAPI (URL/file) if any — the contract API tests and fixtures derive from. If none was provided: observed endpoints/traffic only — weaker, flagged as a GAP to close.>

## Gaps & unexplored

<Everything unknown: skipped inputs and what they block, screens unreachable (missing account/flag), unconfirmed learned rules awaiting user confirmation.>
