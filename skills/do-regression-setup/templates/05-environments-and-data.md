# Environments & Test Data — <test project name>

_Generated by `do-regression-setup` · test-project commit `<hash>` · app build explored `<version/build>` · <YYYY-MM-DD>_

> **No secret values** — credential *names/locations* only.

## Target environments

<Which app environment(s) the suite runs against — from the provided URLs/builds. Record which third parties are **sandbox vs live** per env (ask the user / observe): a regression run must never hit a live third party by accident.>

| Suite / trigger | Target env | Base config (env-var names) | Third parties |
|-----------------|-----------|------------------------------|---------------|
| <smoke, per deploy> | <staging URL / build> | <`QA_BASE_URL`, …> | <sandbox> |
| <full regression, nightly> | <staging> | | <sandbox> |

## Test accounts & authentication

<How tests authenticate **the real way**, with the **user-provided accounts** (one per role — ask for the role list; if unknown, roles observed during exploration, flagged for confirmation): where credentials live (env-var names / secret manager — never values), how a session/token is obtained against the built app, and how auth state is reused across tests (cached session vs per-test login). Missing accounts are a GAP in `01` — flows behind login stay blocked until provided.>

| Account | Role | Purpose | Credentials (name/location only) | Source |
|---------|------|---------|----------------------------------|--------|
| <qa-admin> | <admin> | <full-access flows> | <`QA_ADMIN_USER` / secret manager> | provided |
| <qa-staff> | <staff> | <permission-boundary tests> | | provided |

## Test user catalog — condition cases (derived, not brainstormed)

> One row per condition the **learned model + provided docs** record (see the derivation table in the
> SKILL): every role, every observed entity lifecycle state, every delete/archive rule, every flow
> position, every wizard state, every auth state, and the data extremes. Each row traces to its
> source; every user is a **seed guarantee** (recreated on reset — never improvise state mid-test,
> that's the flaky-test factory). **Completeness check on every refresh:** a known condition with no
> user row is a loud gap; newly observed states/roles flag their missing users here.

| User | Role | Condition it represents | Derived from | Seed guarantee | Used for (coverage rows) |
|------|------|--------------------------|--------------|----------------|--------------------------|
| <qa-fresh> | <staff> | <brand-new, zero data> | <data extremes> | ✓ per reset | <empty states, onboarding> |
| <qa-loaded> | <staff> | <data at realistic max, longest content> | <exploration> | ✓ | <long lists, clipping> |
| <qa-orphan> | <staff> | <owns rows whose parent was archived> | <PRD rule / explored archive flow> | ✓ | <destructive/integrity> |
| <qa-draft> | <staff> | <mid-wizard saved draft> | <explored stepped flow> | ✓ | <resume, abort-clean> |
| <qa-expired> | <—> | <expired token / locked account> | <observed auth behavior> | ✓ | <401/re-login paths> |
| <qa-consumer-empty> | <staff> | <consumer area empty while source has data> | <learned flow map (04)> | ✓ | <cross-feature freshness> |

## Seed / reset strategy

<How each run gets **domain-realistic data** and stays **isolated** — via the **provided seed/reset access** (scripts, DB, admin API) when it exists; otherwise **via the app's own UI/API flows** (scripted creation through the public surface — slower, fully black-box, flagged as the fallback). Reset-then-seed per run? per-test unique data (prefix)? cleanup policy? What the seed guarantees exists — including every condition user above.>

```
# Reset + seed the target env (provided access — or the via-app seeding script)
<command>
```

## Parallelism & isolation

<Can tests run in parallel — and what makes that safe (unique data per worker, no shared mutable fixtures, no order dependence → `08-stability-policy.md`).>
