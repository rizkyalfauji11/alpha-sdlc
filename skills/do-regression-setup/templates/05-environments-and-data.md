# Environments & Test Data — <test project name>

_Generated by `do-regression-setup` · test-project commit `<hash>` · app-profile commit `<hash>` · <YYYY-MM-DD>_

> **No secret values** — credential *names/locations* only.

## Target environments

<Which app environment(s) the regression suite runs against, per the app's `09-environment.md` (sandbox/live per env!). A regression run must never hit a live third party by accident.>

| Suite / trigger | Target env | Base config (env-var names) | Third parties |
|-----------------|-----------|------------------------------|---------------|
| <smoke, per PR> | <ephemeral / dev> | <`QA_BASE_URL`, …> | <sandbox> |
| <full regression, nightly> | <staging> | | <sandbox> |

## Test accounts & authentication

<How tests authenticate **the real way**, per the app's `13-auth.md` → *Test / dev authentication*: the test users (one per role from the app's roles × capabilities matrix), where their credentials live (env-var names / secret manager), how a session/token is obtained, and how auth state is reused across tests (cached session vs per-test login).>

| Account | Role | Purpose | Credentials (name/location only) |
|---------|------|---------|----------------------------------|
| <qa-admin> | <admin> | <full-access flows> | <`QA_ADMIN_USER` / secret manager> |
| <qa-staff> | <staff> | <permission-boundary tests> | |

## Test user catalog — condition cases (derived from the app profile)

> **Enumerated, not brainstormed** — one row per condition the app profile records, so no test case is
> missed by imagination: every **role** (`12-security-compliance`), every **entity lifecycle state** and
> **on-delete edge** (`06-domain-model`), every **flow binding** position (`16-feature-map` + TRD §2),
> every **wizard state** (TRD multi-step specs), every **auth state** (`13-auth`), and the **data
> extremes** (zero / realistic max). Each row traces to the truth that generated it; every user is a
> **seed guarantee** (recreated on reset — never improvise state mid-test, that's the flaky-test
> factory). **Completeness check on every refresh:** a recorded condition with no user row is a loud
> gap; a new state/edge/role in the app profile flags its missing users here.

| User | Role | Condition it represents | Derived from | Seed guarantee | Used for (coverage rows) |
|------|------|--------------------------|--------------|----------------|--------------------------|
| <qa-fresh> | <staff> | <brand-new, zero data> | <data extremes> | ✓ per reset | <empty states, onboarding> |
| <qa-loaded> | <staff> | <data at realistic max, longest content> | <content-fit extremes> | ✓ | <long lists, clipping> |
| <qa-orphan> | <staff> | <owns rows whose parent entity is archived> | <06 on-delete edge <EntityA→B>> | ✓ | <destructive/integrity> |
| <qa-draft> | <staff> | <mid-wizard saved draft> | <TRD flow spec <flow>> | ✓ | <resume, abort-clean> |
| <qa-expired> | <—> | <expired token / locked account> | <13-auth states> | ✓ | <401/refresh paths> |
| <qa-consumer-empty> | <staff> | <consumer feature empty while source seeded> | <flow binding <element>> | ✓ | <create-direction freshness> |

## Seed / reset strategy

<How each run gets **domain-realistic data** (the app's `07-database.md` → Seed / test data) and how runs stay **isolated**: reset-then-seed per run? per-test unique data (prefix/tenant)? cleanup policy? What the seed guarantees exists (parent entities with children so consuming flows have real data).>

```
# Reset + seed the target env
<command>
```

## Parallelism & isolation

<Can tests run in parallel — and what makes that safe (unique data per worker, no shared mutable fixtures, no order dependence → `08-stability-policy.md`).>
