# Test Stack — <test project name>

_Generated by `do-regression-setup` · test-project commit `<hash>` · <YYYY-MM-DD>_

> Versions live in the manifest, not here. Prefer the frameworks the **app's `do-testing` already
> uses** (rung 2) — one stack across feature tests and regression, not two to maintain.

## Framework per platform

| Platform | Framework / runner | Notes |
|----------|--------------------|-------|
| <Web> | <e.g. Playwright + its runner> | |
| <Android> | <Espresso / Compose-UI / Appium> | |
| <iOS> | <XCUITest / Appium> | |
| <API> | <HTTP client + contract checks> | |

## Approved library per concern

> Reuse-before-add lookup, same as the app's `05-tech-stack.md`. A second library for a covered
> concern is drift. New concern → gated decision → register here.

| Concern | Approved library / mechanism | Notes |
|---------|------------------------------|-------|
| <Assertions> | <the runner's built-in> | |
| <API calls in tests> | <typed client generated from the app contract — see 04> | never hand-rolled shapes |
| <Test-data builders> | <fixtures derived from the contract> | domain-realistic, never random |
| <Waiting / sync> | <framework auto-wait + explicit conditions> | no fixed sleeps (→ 08) |
| <Visual / pixel diff> | <e.g. pixelmatch/odiff> | |

## Code generation

<The typed API client / fixture types regenerated from the app's machine-checkable contract (path in `04-app-under-test.md`): tool, command, output location, when to re-run (any contract change).>

```
# Regenerate typed client / fixtures
<command>
```

## Run commands

```
# Full regression suite
<command>
# One suite/tag (e.g. smoke)
<command>
# One spec, headed/debug
<command>
```

<Prerequisites — runtimes, browsers/emulators, env vars (names only).>
