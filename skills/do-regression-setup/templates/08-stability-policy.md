# Stability Policy — <test project name>

_Generated by `do-regression-setup` · test-project commit `<hash>` · <YYYY-MM-DD>_

> A regression suite is only useful while it's **trusted** — one tolerated flaky test and every red
> run becomes "probably flake". This doc is the trust contract.

## Waiting & synchronization

- **Never a fixed sleep.** <The approved mechanisms: framework auto-wait, explicit wait-for-condition (element state, network idle, event) — name them per platform.>
- <Timeout defaults and where they're configured.>

## Determinism

- **No order dependence:** every test passes alone and in any order (unique data per test/worker per `05`).
- **No shared mutable state** between tests; seed guarantees, not leftovers.
- <Clock/locale/viewport pinned where rendering depends on them.>

## Retry policy

<Retries hide real races — the policy: e.g. max 1 retry in CI only; a test that passes on retry is logged as **flaky**, not green; zero retries locally.>

## Flaky-test quarantine

<The flow: flaky (fails intermittently with no app change) → tagged `@quarantine` (runs, doesn't gate) + an issue with the failure trace → **root-caused within <N days> — fixed or deleted**. Quarantined tests are listed in run reports so they can't be forgotten; a quarantined test's coverage row (10) shows the gap it leaves.>

## Runtime budget

<Per-suite budgets (from `07`) and the rule when a suite outgrows its budget — parallelize, prune redundant tests, or split — decided with the user, never by silently skipping.>
